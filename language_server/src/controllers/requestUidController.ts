import * as vscode from 'vscode';
import {httpRequestFactory} from '../utility/httpRequestFactory';

export class requestUidController {

    static sync getCompletionItem(partUid: string): void{
        return ;
    }
}